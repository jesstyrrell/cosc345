// PlayerProfile.h
#ifndef PLAYER_PROFILE_H
#define PLAYER_PROFILE_H

#include <string>

struct PlayerProfile {
    std::string name;
    int age;
    std::string position;
    double height;
    double weight;
};

#endif // PLAYER_PROFILE_H